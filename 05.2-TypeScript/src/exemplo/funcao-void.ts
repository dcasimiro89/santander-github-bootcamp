function somarValores(numero1: number, numero2: number): number {
    return numero1 + numero2;
}

function printaValoresNumericos(num1: number, num2: number): void { // função que não retorna nada
    console.log(num1, num2);
}

const teste = console.log(3) // não tem retorno
// resposta será: undefined